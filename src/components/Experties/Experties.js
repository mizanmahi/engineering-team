import React from 'react';

const Expertise = ({ children }) => {
   return (
      <span className='text-main bg-bg p-2 rounded uppercase font-bold text-xs'>
         {children}
      </span>
   );
};

export default Expertise;
